# Módulos / funciones

```mermaid
flowchart LR
    M["main.py"]
    M --> P["pedir_entero(mensaje, min, max)"]
    M --> R["resultado_ronda(usuario, pc)"]
    M --> L["Menú (while True)"]
    M --> F["for rondas 1..N"]
